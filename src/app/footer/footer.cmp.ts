import { Component, ElementRef, Renderer, ViewChild, Input, OnInit } from '@angular/core';

@Component({
	moduleId: module.id,
	selector: 'rossana-footer',
	templateUrl: 'footer.tmp.html'
})

export class MainFooter implements OnInit{

	constructor(){
	}

	ngOnInit(){
	}

}