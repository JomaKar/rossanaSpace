import { Component } from '@angular/core';

@Component({
	moduleId: module.id,
	selector: 'news-now',
	templateUrl: 'newsPresent.tmp.html'
})
export class newsNowComponent {
  title = 'news now space!';
}
